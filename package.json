{
  "private": true,
  "name": "conventional-changelog-tools",
  "version": "0.0.0",
  "repository": "https://github.com/Oriflame/conventional-changelog-tools",
  "author": {
    "name": "Jan Silhan",
    "email": "silhanu@gmail.com"
  },
  "license": "MIT",
  "scripts": {
    "prepare": "lumos create-config && husky install",
    "build": "lumos typescript --build --reference-workspaces",
    "coverage": "yarn run jest --coverage",
    "lint": "lumos eslint",
    "jest": "lumos jest -i",
    "prettier": "lumos prettier",
    "type": "yarn run build",
    "pretest": "yarn run type",
    "test": "yarn run jest",
    "posttest": "yarn run lint",
    "release-alpha": "git reset --hard && lerna publish --canary --force-publish --yes",
    "release": "git reset --hard && lumos run-script auto-release",
    "link-packages": "lerna exec -- yarn link",
    "unlink-packages": "lerna exec -- yarn unlink"
  },
  "workspaces": [
    "./packages/*"
  ],
  "lumos": {
    "drivers": [
      "babel",
      "eslint",
      "jest",
      "prettier",
      "typescript"
    ],
    "settings": {
      "node": true,
      "library": true
    }
  },
  "devDependencies": {
    "@commitlint/cli": "^13.2.1",
    "@oriflame/config-babel": "^4.0.11",
    "@oriflame/config-danger": "4.0.7",
    "@oriflame/config-eslint": "^4.0.11",
    "@oriflame/config-jest": "^4.0.10",
    "@oriflame/config-prettier": "^4.0.2",
    "@oriflame/config-typescript": "^4.0.2",
    "@oriflame/lumos": "^4.0.11",
    "@types/node": "^14.17.27",
    "@types/shelljs": "^0.8.9",
    "husky": "^7.0.4",
    "lerna": "^4.0.0",
    "shelljs": "^0.8.4"
  },
  "resolutions": {
    "caniuse-lite": "^1.0.30001271"
  }
}
